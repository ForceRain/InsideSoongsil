#include <stdio.h>/*
int main(void)
{
	    char s[100]={0};
		    int i;
			scanf("%s",s);
			    for(i=0; i<100; i++)
					    {
							        if(s[i]>'Z')
										            s[i]=s[i]-('a'-'A');
									        else
												            s[i]=s[i]+('a'-'A');
											    }
				    printf("%s",s);
}*/
